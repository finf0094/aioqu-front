export enum RoutePath {
  HOME = "/",
  Overview = "/overview",
  MyCourses = "/my-courses",
  Courses = "/courses",
  Mentors = "/mentors",
  Settings = "/settings",
  Course = "/course/:courseId",
  Module = "/module/:moduleId",
}
